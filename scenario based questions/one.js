console.log(2 + true);

output:
3

reason:
whenever + operator is found along with the number it tries to convert the next operand to the same type that is
here it tries to convert true to it's numeric equivalent that is 1.
